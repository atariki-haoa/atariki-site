services:
  web:
    build: .
    image: atariki-site:latest
    container_name: atariki-site
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "127.0.0.1:3000:3000"
    volumes:
      - blog-data:/app/data

volumes:
  blog-data:
